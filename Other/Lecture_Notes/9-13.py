'''
Document String:
    Put these right after the line that a function is defined. 
    Typically at the top of the file. 

'''

# Data Types

# Tuple
    # Benefit of tuples are that they dont change (immutable)
    # if you have a tuple with a single value you need to have a , after that value
    
my_tuple = (1, 2, 3)
    
print(type(my_tuple))
print(my_tuple)
print(my_tuple[0])

# tuple() a constructor for tuples
new_tup = tuple((1,2,4))
print(new_tup)

list_t0_tupe = 

